(() => {
  const aoany = require('./aoa-ny/aoa-ny.md');
  const bluechairbay = require('./bluechairbay/bluechairbay.md');
  const ciroc = require('./ciroc/ciroc.md');
  const citizen = require('./citizen/citizen.md');
  const davidsbridal = require('./davids-bridal/davids-bridal.md');
  const gshock = require('./g-shock/g-shock.md');
  const hard = require('./hard/hard.md');
  const macys = require('./macys/macys.md');
  const mtv = require('./mtv/mtv.md');
  const tambaran = require('./tambaran/tambaran.md');
  const wwglass = require('./wwglass/wwglass.md');

  return {
    aoany,
    bluechairbay,
    ciroc,
    citizen,
    davidsbridal,
    gshock,
    hard,
    macys,
    mtv,
    tambaran,
    wwglass
  };
})();
