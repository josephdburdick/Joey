// Deployed to GitHub Pages at https://josephdburdick.github.io/Joey/
// If a custom domain (e.g. j0ey.co) is added, set base to '/' and update site.
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://josephdburdick.github.io',
  base: '/Joey',
  trailingSlash: 'ignore',
});
